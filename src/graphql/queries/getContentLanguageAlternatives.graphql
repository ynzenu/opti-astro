query getContentLanguageAlternatives($key: [String]!) {
    _Content(
        ids: $key
        locale: [ALL]
    ) {
        items {
            _metadata {
                key
                locale
                url {
                    default
                    hierarchical
                }
            }
        }
    }
}
