query getAvailableLocales {
    _Content(
        locale: [ALL]
        limit: 100
    ) {
        facets {
            _metadata {
                locale(limit: 50) {
                    name
                    count
                }
            }
        }
    }
}
